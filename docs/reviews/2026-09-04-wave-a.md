# Волна A — поверхность оператора, 2026-09-04

Ветка `lane/plan-2026-09-04` от master `99e4f8e`. План:
`~/.claude/plans/ahde-ideal-product-2026-09-04.md` (свод четырёх ревью: gstack
`plan-eng-review`, mattpocock `improve-codebase-architecture`, cursor
`thermo-nuclear-code-quality-review`, продуктовое «10-звёздочная версия»).
Параллельный срез Codex (validation split, verified evidence, цена Target +
Builder, argv, executed vs reported tools) лежит незакоммиченным в рабочем
дереве master и в эту ветку не входит; слияние — после его коммита.

## Что стало правдой

Девять полос, каждая от `99e4f8e` в своём worktree, слиты `--no-ff`; все
конфликты — append в `src/i18n.ts`, разрешены keep-both.

| Полоса | Что теперь верно | Доказательство |
|---|---|---|
| `passport-ru` | Паспорт (markdown, который читает начальник) — по-русски: «Обещано / Измерено / Судья / Чего мы не знаем / Откуда взялось / Происхождение»; хэши в подвале; первая версия без `→`; причина выката Билдера цитируется как цитата | `version-passport.ts`: 0 → 122 `t(`; `passport-v0.1.0.md` сессии 8 перерисован новым кодом |
| `compare-honesty` | Одна инфра-ошибка стоит кейса, а не всей проверки: задачи с ошибочным повтором исключаются из обоих плеч, сравнение идёт при ≤ 10 % (`INFRASTRUCTURE_ERROR_BUDGET`); строка «на 14 из 15 кейсов × 5 · 1 исключён: инфраструктура»; колонка `3/3`, ошибочный повтор ≠ pass, при k=1 «шум не измерен»; экзамен называет автора; `/dataset` «выгружено N из M» | `compare.ts`, `comparison-gate.ts` (`taskExclusion`, `comparisonPowered`), `measurement-line.ts`; паспорт сессии 8 показывает «14 из 15 · 1 исключён» |
| `exam-kb` | База из трёх документов даёт полный экзамен (≤ 3 вопроса на отрывок, при малой базе отрывки мельче, геометрия в receipt); отказ с числом до траты денег: «В базе 3 фрагмента — из неё выходит не больше 9 вопросов, экзамену нужно 15 кейсов. Могу написать экзамен из описания (20 кейсов) — делаем?»; `DEFAULT_SEALED_SYNTH_EXAMPLES` 5 → 0; receipt пишется до удаления exchange | `sealed-synth.ts`, `domain/kb.ts`; `maxKbQuestions` в view |
| `i18n-reasons` | «Почему» говорит, чего хотела каждая проверка, по-русски (`why.expected.<code>` / `why.actual.<code>` на каждый check code); причина `world_state` рендерится из структуры; отказы `candidate.ts`, `improvement-brief.ts`, `inventory.ts` — типизированные; гвард `leakedEnglish` по всем `ru`-значениям + паритет плейсхолдеров (нашёл пять расхождений); `TABLES` без `Partial`; `tokenLabel` вместо трёх кастов | `tests/i18n.test.ts`; `grep 'expected: "' run-explanation.ts` пуст |
| `render-names` | Кейс назван своими словами («Скорость интернета низкая, договор 1003»), id рядом; «кто» из allowlist путей и карт (`accounts.1003` → «клиент по счёту 1003»), секретные ключи не печатаются; черновик рисует те же карточки, что `/traces`; заголовок «применена… не запустилась» без дубля; регресс-тест стабильности id кейса | `builder-render.test.ts`, `world.test.ts` |
| `workshop-restart` | Мастерская видна после перезапуска TUI: `workshop: live / recorded / stale` из host note, `next` предлагает `workshop-open` с id; `run-current` вынесен в `decisions/`, `decide` — чистая делегация | `workbench.test.ts` (suspend → новый workbench → recorded → reattach) |
| `gate-dialog` | Один список запрещённых решений с брендом гейта; `ahde improve`/`search` из CLI под гейтом (дыра инварианта 6 закрыта); `choose()` с id — `/fix`, `/review`, `/holdout`, диалоги подключения не диспатчат по строке | `restricted-gate.test.ts`, `builder-dialog.test.ts` |
| `help-slim` | `/help` — девять строк в голосе «что сказать»; `/help all` — три яруса (каждый день / экспертные / решения хоста); команда без яруса падает при регистрации | снимок `/help` ниже |
| `thermo-small` | `ContainerRuntimeStatus` — union (кастов 25 → 2, два разошедшихся гарда идентичности сведены в один); re-attach worktree в модуле-владельце; NUL-байт в `export-dataset.ts` | `container-backend.test.ts` |

Три правки по следам смоука: «кто» для миров-карт (`accounts.<id>`); «за 1
попытку» (винительный); `exports/` и `passport-*.md` — выход хоста, а не
незакоммиченная работа оператора, и отказ `/target` по-русски (сессия 8 после
выката и `/dataset` получала английское «Target has uncommitted changes» за два
файла, которые AHDE сам только что написал).

## Гейт

| Проверка | Результат |
|---|---|
| `npm run check` | 130 файлов · 2161 тестов · 3 пропущены (Docker daemon недоступен) |
| `npm run build` | зелёный |
| `npm run verify:package` | зелёный после одной правки: комментарий с бэктиками внутри template-литерала consumer-программы в `scripts/verify-package.mjs`. Замечание: в worktree с symlink-ом `node_modules` `npm pack` теряет транзитивные зависимости bundled-пакетов (`chalk`); гейт гонять на настоящем `npm ci` |

## Смоук без модели

Стенд: папка сессии 8 (`live-8/agent`, выкачена v0.1.0), TUI из `dist`
ветки, PTY-снимки. Первый экран, `/help`, `/help all`, `/status`, карточки
`/traces`, `/passport`, `/log` — по-русски, числа с провенансом. `/help`:

```
◆ Справка AHDE Билдера
  Говори обычными словами: опиши агента, отвечай по одному полезному вопросу
  за раз — AHDE сам превратит разговор в описание, тесты, прогоны, разбор
  и точные правки. Команды ниже — сокращения, а не обязанность.
    /test        прогони тесты: опубликую всё, что ждёт, или проверю правку
    /fix 2       почини вторую проблему: обновлю трейсы и покажу диф
    /ship 0.1.0  выкати проверенного кандидата и начни новый круг
    /status      где ты и что сказать дальше
    /traces      разбор: типы сбоев, ссылка на данные и таблица прогонов
    /trace 3     один прогон целиком: почему провал, вердикты и диалог
    /passport    что обещала и что измерила выкаченная версия
    /dataset     все записанные диалоги одним файлом рядом с агентом
    /help all    все команды, вместе с сокращениями
```

Паспорт сессии 8, перерисованный новым кодом, «Измерено»:

```
- Разработка: стало лучше · балл 73% → 85% (+12.4 п.п., 95% ДИ +4.8 … +20.2) на 14 из 15 кейсов × 5 · 1 исключён: инфраструктура · пасс-рейт 61% → 71%
- Закрытый экзамен: пройден · ухудшения не доказано, улучшения тоже на 20 × 5 · содержимое видит только оценщик
- Обещано +8.9 п.п. (пасс-рейт) · получено +10 п.п.
```

## Найдено, не исправлено (в план)

1. **Происхождение кандидата хранит абсолютные пути.** Скопированная в другое
   место папка агента получает на первом экране «Что-то на диске больше не
   сходится со своей записью — решать ничего нельзя» (`inventory.ts:204`,
   `candidate provenance points at an unexpected artifact path`). Идентичность —
   sha256, путь должен быть относительным к `runs/`; старые записи сверять по
   суффиксу. Волна B.
2. **`/traces` заканчивается диалогом «Готовить правку?»** с платным пунктом
   первым: случайный Enter запускает ход Билдера за деньги. Первым должно стоять
   «Не сейчас».
3. `exports/` не в сгенерированном `.gitignore` (в `/runs/`, `/imports/` есть).
4. В таблице прогонов колонка «кейс» всё ещё id (`task-0caf4a4b…`): сетка 110
   колонок, титул не влезает; карточки выше уже названы словами.
5. Дата в паспорте — сырой ISO (`2026-09-03T18:31:32.657Z`).
6. Строка шума на панели паспорта печатает `pp` — полоса `measurement` (A2).

## Что дальше

Полоса `measurement` (единицы измерения в одном модуле), затем слияние с
коммитом Codex, затем сессия 9 — чужой оператор на той же папке, критерии в
плане («Приёмка»).
